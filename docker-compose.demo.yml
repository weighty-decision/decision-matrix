# Docker compose file for demo usage
# See the README for available parameters

services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: decision_matrix
      POSTGRES_USER: decision_matrix
      POSTGRES_PASSWORD: decision_matrix_password
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U decision_matrix -d decision_matrix"]
      interval: 2s
      timeout: 3s
      retries: 5

  decision-matrix:
    image: ghcr.io/weighty-decision/decision-matrix:latest # don't use latest in production. use a specific version in for stability
    ports:
      - "8080:8080"
    environment:
      - DM_DEV_MODE=true
      - DM_INCLUDE_SAMPLE_DATA=true
      - DM_LOG_FORMAT=CONSOLE # Options are CONSOLE, JSON, ECS
      - DM_LOG_LEVEL=INFO  # Choose a logback log level of TRACE, DEBUG, INFO, WARN, ERROR
      - DM_INCLUDE_SAMPLE_DATA=true
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_USER=decision_matrix
      - DB_PASSWORD=decision_matrix_password
      - DB_NAME=decision_matrix
    depends_on:
      postgres:
        condition: service_healthy
